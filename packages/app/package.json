{
  "name": "vocal-recorder",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "vite build",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "prepublish": "pnpm build",
    "deploy": "pnpm bumpp --no-push && pnpm publish --tag alpha"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/audioworklet": "^0.0.35",
    "standardized-audio-context-mock": "^9.6.8",
    "typescript": "^4.9.4",
    "vite-plugin-dts": "^1.7.1"
  },
  "dependencies": {
    "vmsg-worker": "workspace:*"
  },
  "publishConfig": {
    "directory": "dist"
  }
}
